[project]
name = "planner"
version = "0.1.0"
description = "Tyre-plant production planning engine (PCR + TBR)"
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"

[tool.ruff]
line-length = 110
target-version = "py311"
